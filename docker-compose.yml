version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: delivery-redis-instance

    ports:
      - "6379:6379"

    volumes:
      - redis_data:/data

    restart: always
    
volumes:
    redis_data:
